extends layout

block content
  h1= title
  - var image_names = ['chevy','rivian','volkswagen','winnebago','sprinter']
     -for(var i=0; i<3; i++){
         - var rand = Math.floor(Math.random()*5);
           img(src=`/images/${image_names[rand]}.jpg`, "height"=300, "width"=300)
     -}